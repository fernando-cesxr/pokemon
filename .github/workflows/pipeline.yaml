name: pokemon-pipeline
on:
  push:
    branches:
      - main
jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setting up java plugin...
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          architecture: x64
      - name: Installing dependencies...
        run: mvn clean install -Ddb_url={{ secrets.DB_URL }} -Ddb_password={{ secrets.DB_PASSWORD }} -Dsecret={{ secrets.SECRET }} -Ddb_user={{ secrets.DB_USER }}
  build:
    needs: install
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setting up java plugin...
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          architecture: x64
      - name: Building java application
        run: mvn clean package -Ddb_url={{ secrets.DB_URL }} -Ddb_password={{ secrets.DB_PASSWORD }} -Dsecret={{ secrets.SECRET }} -Ddb_user={{ secrets.DB_USER }}
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4